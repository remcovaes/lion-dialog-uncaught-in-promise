{
  "name": "lion-dialog-uncaught-in-promise",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "build": "rollup -c && cp src/index.html dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "http-server -c-1 dist"
  },
  "devDependencies": {
    "http-server": "^0.12.3",
    "rimraf": "^3.0.2",
    "rollup": "^2.18.0",
    "rollup-plugin-node-resolve": "^5.2.0"
  },
  "dependencies": {
    "@lion/dialog": "^0.7.4",
    "lit-element": "^2.3.1"
  }
}
